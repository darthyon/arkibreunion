# Convex — set automatically by `npx convex dev` (writes NEXT_PUBLIC_CONVEX_URL
# to .env.local). Convex Auth keys (JWKS, JWT_PRIVATE_KEY, SITE_URL) live in the
# Convex deployment, not here — `npx @convex-dev/auth` provisions them.
NEXT_PUBLIC_CONVEX_URL=
