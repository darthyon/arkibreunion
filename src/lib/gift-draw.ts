import type { GiftAssignment, GiftParticipant } from "@/types/gift-exchange";

export function createGiftAssignments(participants: GiftParticipant[]): GiftAssignment[] {
  void participants;

  throw new Error("Gift draw logic will be implemented in the Tukar Hadiah phase.");
}
